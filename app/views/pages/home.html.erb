  <div class="container mt-4 p-4" data-controller="landing">
    <div class="row justify-content-center">
      <div class="col-md-4 border shadow-sm mt-4 p-4">
        <div class=" text-center">
          <h1><strong>CineSelect</strong></h1>
        </div>
      </div>
    </div>
    <div class="container">
      <div class="row">
        <div class="col mt-4 text-center">
          <h2 class="mt-4"><strong>Vos critères, notre sélection!</strong></h2>
        </div>
      </div>
      <% if current_user %>
      <div class="edit-profile text-center mt-3">
        <%= link_to "Modifier le profile", profile_path(current_user), class: "btn btn-primary rounded" %>
      </div>
      <% end %>
    </div>
  </div>
  </div>
  </div>
